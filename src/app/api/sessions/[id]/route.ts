// Temporary alias (Sprint 1). Canonical route is /api/episodes/[id].
// Kept so any existing links to /api/sessions keep working; remove in a later sprint.
export { GET, PATCH } from "@/app/api/episodes/[id]/route";
